import { useEffect, useMemo, useState } from 'react'
import { Link, NavLink, Navigate, Route, Routes, useLocation, useNavigate, useParams } from 'react-router-dom'
import {
  ArrowRight, CalendarDays, CheckCircle2, ChevronLeft, CircleUserRound, Clock3, LogOut,
  MapPin, Menu, Search, ShieldCheck, Trophy, UserRound, Users, X, Zap,
} from 'lucide-react'
import { api, authApi, getStoredUser, setStoredAuth } from './lib/api'
import './index.css'

const fallbackSessions = [
  { id: 1, title: 'Weekend Turf Cricket', venue: 'Noida Sports Arena', location: 'Sector 62, Noida', date: 'Sat, 28 Sep', time: '7:00 AM - 10:00 AM', spots: 5, level: 'Intermediate', price: '₹299' },
  { id: 2, title: 'Corporate Evening Match', venue: 'PlayGround 11', location: 'Indirapuram, Ghaziabad', date: 'Sun, 29 Sep', time: '6:00 PM - 9:00 PM', spots: 8, level: 'All levels', price: '₹249' },
  { id: 3, title: 'Sunday Leather Ball', venue: 'Cricket Hub', location: 'Greater Noida West', date: 'Sun, 29 Sep', time: '7:30 AM - 11:00 AM', spots: 3, level: 'Advanced', price: '₹399' },
]
const venueImages = [
  'https://images.unsplash.com/photo-1531415074968-036ba1b575da?auto=format&fit=crop&w=900&q=85',
  'https://images.unsplash.com/photo-1540747913346-19e32dc3e97e?auto=format&fit=crop&w=900&q=85',
  'https://images.unsplash.com/photo-1552674605-db6ffd4facb5?auto=format&fit=crop&w=900&q=85',
  'https://images.unsplash.com/photo-1461896836934-ffe607ba8211?auto=format&fit=crop&w=900&q=85',
  'https://images.unsplash.com/photo-1579952363873-27f3bade9f55?auto=format&fit=crop&w=900&q=85',
  'https://images.unsplash.com/photo-1521412644187-c49fa049e84d?auto=format&fit=crop&w=900&q=85',
]

function Layout({ children }) {
  const [open, setOpen] = useState(false)
  const user = getStoredUser()
  const navigate = useNavigate()
  const logout = () => { setStoredAuth(null); navigate('/') }
  return <div className="min-h-screen bg-slate-50">
    <header className="sticky top-0 z-50 border-b border-slate-200/80 bg-white/90 backdrop-blur">
      <div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 lg:px-8">
        <Link to="/" className="flex items-center gap-2.5" onClick={() => setOpen(false)}>
          <span className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-600 text-xl shadow-sm">🏏</span>
          <div><div className="text-lg font-extrabold tracking-tight text-slate-950">Cricket<span className="text-emerald-600">Connect</span></div><div className="hidden text-[10px] font-semibold uppercase tracking-[0.18em] text-slate-400 sm:block">Play. Find. Connect.</div></div>
        </Link>
        <nav className="hidden items-center gap-7 md:flex"><NavItem to="/">Home</NavItem><NavItem to="/sessions">Find Sessions</NavItem><NavItem to="/venues">Venues</NavItem><NavItem to="/bookings">My Bookings</NavItem></nav>
        <div className="hidden items-center gap-3 md:flex">{user ? <><Link to="/dashboard" className="rounded-xl px-3 py-2 text-sm font-bold text-slate-700 hover:bg-slate-100">Dashboard</Link><button onClick={logout} className="rounded-xl border border-slate-200 px-3 py-2 text-sm font-bold text-slate-600">Log out</button></> : <><Link to="/login" className="rounded-xl px-4 py-2 text-sm font-bold text-slate-700 hover:bg-slate-100">Log in</Link><Link to="/register" className="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white shadow-sm hover:bg-slate-800">Get started</Link></>}</div>
        <button className="rounded-xl p-2 md:hidden" onClick={() => setOpen(!open)} aria-label="Toggle menu">{open ? <X /> : <Menu />}</button>
      </div>
      {open && <div className="border-t border-slate-100 bg-white px-5 py-4 md:hidden"><div className="grid gap-1"><MobileNav to="/" onClick={() => setOpen(false)}>Home</MobileNav><MobileNav to="/sessions" onClick={() => setOpen(false)}>Find Sessions</MobileNav><MobileNav to="/venues" onClick={() => setOpen(false)}>Venues</MobileNav><MobileNav to="/bookings" onClick={() => setOpen(false)}>My Bookings</MobileNav><MobileNav to={user ? '/dashboard' : '/login'} onClick={() => setOpen(false)}>{user ? 'Dashboard' : 'Log in'}</MobileNav></div></div>}
    </header>
    {children}
    <footer className="border-t border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-5 py-8 text-sm text-slate-500 sm:flex-row sm:items-center sm:justify-between lg:px-8"><div>© 2026 CricketConnect. Built for local cricket communities.</div><div className="flex gap-5"><Link to="/sessions" className="hover:text-slate-900">Sessions</Link><Link to="/venues" className="hover:text-slate-900">Venues</Link><Link to="/register" className="hover:text-slate-900">Join</Link></div></div></footer>
  </div>
}

function NavItem({ to, children }) { return <NavLink to={to} end={to === '/'} className={({ isActive }) => `text-sm font-semibold ${isActive ? 'text-emerald-600' : 'text-slate-600 hover:text-slate-950'}`}>{children}</NavLink> }
function MobileNav({ to, onClick, children }) { return <NavLink to={to} onClick={onClick} className="rounded-xl px-3 py-3 text-sm font-semibold text-slate-700 hover:bg-slate-50">{children}</NavLink> }
function Page({ title, subtitle, children, eyebrow = 'Cricket Connect' }) { return <main className="mx-auto min-h-[calc(100vh-128px)] max-w-7xl px-5 py-12 lg:px-8"><div className="mb-8"><div className="text-sm font-bold uppercase tracking-widest text-emerald-600">{eyebrow}</div><h1 className="mt-2 text-4xl font-black tracking-tight text-slate-950">{title}</h1><p className="mt-2 text-slate-500">{subtitle}</p></div>{children}</main> }
function State({ loading, error, empty, children }) { if (loading) return <div className="grid min-h-48 place-items-center rounded-2xl border border-slate-200 bg-white"><div className="text-sm font-semibold text-slate-500"><span className="mr-2 inline-block h-4 w-4 animate-spin rounded-full border-2 border-emerald-500 border-t-transparent align-[-2px]" />Loading…</div></div>; if (error) return <div className="rounded-2xl border border-red-200 bg-red-50 p-6 text-sm text-red-700">{error}</div>; if (empty) return <div className="rounded-2xl border border-slate-200 bg-white p-10 text-center"><Trophy className="mx-auto h-10 w-10 text-slate-300" /><h3 className="mt-4 font-extrabold">Nothing here yet</h3><p className="mt-2 text-sm text-slate-500">Check back soon for new cricket activity.</p></div>; return children }
function Info({ icon, text }) { return <div className="flex items-center gap-3 text-emerald-50">{icon && <span className="h-4 w-4 [&>svg]:h-4 [&>svg]:w-4">{icon}</span>}{text}</div> }
function DetailRow({ icon, children }) { return <div className="flex items-center gap-3 text-slate-600"><span className="text-emerald-600 [&>svg]:h-4 [&>svg]:w-4">{icon}</span>{children}</div> }
function Feature({ icon, title, text }) { return <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid h-11 w-11 place-items-center rounded-xl bg-emerald-50 text-emerald-600 [&>svg]:h-5 [&>svg]:w-5">{icon}</div><h3 className="mt-5 text-lg font-extrabold">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></div> }
function SessionCard({ session }) { return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><div className="flex items-start justify-between gap-4"><div><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[11px] font-bold text-emerald-700">{session.level || 'All levels'}</span><h3 className="mt-3 font-extrabold text-slate-950">{session.title || session.name}</h3></div><span className="text-lg font-black text-slate-950">{session.price || session.fee || 'Free'}</span></div><div className="mt-5 space-y-2.5 text-sm text-slate-500"><div className="flex gap-2"><MapPin className="h-4 w-4 shrink-0 text-slate-400" />{session.venue || session.venue_name || 'Venue to be announced'}{session.location ? ` · ${session.location}` : ''}</div><div className="flex gap-2"><CalendarDays className="h-4 w-4 shrink-0 text-slate-400" />{session.date || session.start_date || 'Date to be announced'}</div><div className="flex gap-2"><Clock3 className="h-4 w-4 shrink-0 text-slate-400" />{session.time || session.start_time || 'Time to be announced'}</div></div><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><span className="text-xs font-semibold text-slate-400">{session.spots ?? session.available_spots ?? '—'} spots left</span><Link to={`/sessions/${session.id}`} className="text-sm font-extrabold text-emerald-600">View →</Link></div></article> }

function Home() { return <main><section className="relative overflow-hidden bg-slate-950 text-white"><div className="absolute inset-0 bg-[radial-gradient(circle_at_80%_20%,rgba(16,185,129,.24),transparent_32%),radial-gradient(circle_at_20%_80%,rgba(59,130,246,.12),transparent_30%)]" /><div className="relative mx-auto grid max-w-7xl gap-12 px-5 py-20 lg:grid-cols-[1.05fr_.95fr] lg:items-center lg:px-8 lg:py-28"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3.5 py-2 text-xs font-bold text-emerald-300"><Zap className="h-3.5 w-3.5" /> Your local cricket community</div><h1 className="max-w-3xl text-5xl font-black tracking-tight sm:text-6xl lg:text-7xl">Find your team.<br /><span className="text-emerald-400">Play your game.</span></h1><p className="mt-6 max-w-2xl text-lg leading-8 text-slate-300">Discover nearby cricket sessions, meet players, book venues, and build your cricket community — all in one place.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><Link to="/sessions" className="inline-flex items-center justify-center gap-2 rounded-xl bg-emerald-500 px-5 py-3.5 font-extrabold text-slate-950 hover:bg-emerald-400">Find a session <ArrowRight className="h-4 w-4" /></Link><Link to="/register" className="inline-flex items-center justify-center rounded-xl border border-white/15 bg-white/5 px-5 py-3.5 font-bold hover:bg-white/10">Create your profile</Link></div><div className="mt-10 flex flex-wrap gap-x-8 gap-y-3 text-sm text-slate-400"><span className="flex items-center gap-2"><CheckCircle2 className="h-4 w-4 text-emerald-400" /> Local sessions</span><span className="flex items-center gap-2"><CheckCircle2 className="h-4 w-4 text-emerald-400" /> Easy booking</span><span className="flex items-center gap-2"><CheckCircle2 className="h-4 w-4 text-emerald-400" /> Player discovery</span></div></div><div className="relative"><div className="absolute -inset-4 rounded-[2rem] bg-emerald-400/10 blur-2xl" /><div className="relative rounded-[2rem] border border-white/10 bg-white/10 p-3 shadow-2xl backdrop-blur"><div className="overflow-hidden rounded-[1.4rem] bg-gradient-to-br from-emerald-500 to-emerald-800 p-7"><div className="flex items-start justify-between"><div><div className="text-xs font-bold uppercase tracking-widest text-emerald-100">Next session</div><div className="mt-2 text-2xl font-black">Weekend Turf Cricket</div></div><div className="rounded-xl bg-white/15 px-3 py-2 text-center"><div className="text-xs text-emerald-100">SPOTS</div><div className="text-xl font-black">5</div></div></div><div className="mt-8 grid gap-3 text-sm"><Info icon={<MapPin />} text="Noida Sports Arena · Sector 62" /><Info icon={<CalendarDays />} text="Saturday · 28 September" /><Info icon={<Clock3 />} text="7:00 AM - 10:00 AM" /></div><div className="mt-8 flex items-center justify-between border-t border-white/15 pt-5"><div><div className="text-xs text-emerald-100">Entry</div><div className="text-xl font-black">₹299</div></div><Link to="/sessions" className="rounded-xl bg-white px-4 py-2.5 text-sm font-extrabold text-emerald-800">View session</Link></div></div></div></div></div></section><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="grid gap-5 md:grid-cols-3"><Feature icon={<Search />} title="Discover" text="Find sessions and cricket communities around your location." /><Feature icon={<CalendarDays />} title="Book" text="Reserve a spot or venue without endless WhatsApp coordination." /><Feature icon={<Users />} title="Connect" text="Meet players with similar interests, skill levels and schedules." /></div></section><section className="border-y border-slate-200 bg-white"><div className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="text-sm font-bold uppercase tracking-widest text-emerald-600">Play nearby</div><h2 className="mt-2 text-3xl font-black tracking-tight text-slate-950">Popular sessions</h2></div><Link to="/sessions" className="inline-flex items-center gap-2 text-sm font-extrabold text-slate-700 hover:text-emerald-600">See all <ArrowRight className="h-4 w-4" /></Link></div><div className="mt-8 grid gap-5 lg:grid-cols-3">{fallbackSessions.map(s => <SessionCard key={s.id} session={s} />)}</div></div></section></main> }

function Sessions() { const [items, setItems] = useState([]); const [query, setQuery] = useState(''); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); useEffect(() => { api.sessions().then(setItems).catch(e => setError(e.message)).finally(() => setLoading(false)) }, []); const filtered = useMemo(() => (items.length ? items : fallbackSessions).filter(s => `${s.title} ${s.name} ${s.venue} ${s.location}`.toLowerCase().includes(query.toLowerCase())), [items, query]); return <Page title="Find cricket sessions" subtitle="Discover games happening around you and reserve your spot."><div className="mb-7 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search by session, venue or location" className="w-full rounded-xl border border-slate-200 bg-white py-3.5 pl-11 pr-4 outline-none focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/10" /></div></div><State loading={loading} error={error} empty={!filtered.length}>{<div className="grid gap-5 lg:grid-cols-3">{filtered.map(s => <SessionCard key={s.id} session={s} />)}</div>}</State></Page> }
function Venues() { const [items, setItems] = useState([]); const [error, setError] = useState(''); const user = getStoredUser(); useEffect(() => { if (user) api.venues().then(setItems).catch(e => setError(e.message)) }, [user]); const venues = items.length ? items : ['Noida Sports Arena', 'PlayGround 11', 'Cricket Hub', 'Stadium Cricket Ground', 'The Batting Nets', 'City Cricket Club'].map((name, i) => ({ id: i + 1, name, location: ['Sector 62, Noida', 'Indirapuram, Ghaziabad', 'Greater Noida West'][i % 3] })); return <Page title="Cricket venues" subtitle="Browse places where the community plays."><State error={error} empty={!venues.length}><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{venues.map((v, index) => <div key={v.id} className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="relative h-40 overflow-hidden bg-gradient-to-br from-emerald-100 via-slate-100 to-emerald-50 p-5"><img src={venueImages[index % venueImages.length]} alt={`${v.name || v.title} cricket ground`} className="absolute inset-0 h-full w-full object-cover" loading="lazy" onError={event => { event.currentTarget.style.display = 'none' }} /><div className="relative flex h-full items-end"><span className="rounded-lg bg-white/90 px-3 py-1.5 text-xs font-bold text-slate-700">Cricket venue</span></div></div><div className="p-5"><h3 className="font-extrabold">{v.name || v.title}</h3><p className="mt-2 flex items-center gap-2 text-sm text-slate-500"><MapPin className="h-4 w-4" /> {v.location || v.address || 'Location available in details'}</p><Link to={`/venues/${v.id}`} className="mt-5 block w-full rounded-xl border border-slate-200 py-2.5 text-center text-sm font-bold hover:bg-slate-50">View venue</Link></div></div>)}</div></State></Page> }
function Bookings() { const [items, setItems] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { api.bookings().then(setItems).catch(() => {}).finally(() => setLoading(false)) }, []); return <Page title="My bookings" subtitle="Keep track of your upcoming cricket sessions."><State loading={loading} empty={!items.length}><div className="grid gap-4">{items.map(b => <div key={b.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex justify-between"><h3 className="font-extrabold">{b.session_name || b.session?.title || 'Cricket booking'}</h3><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{b.status || 'Confirmed'}</span></div><p className="mt-2 text-sm text-slate-500">{b.date || b.session?.date || 'Upcoming session'}</p></div>)}</div></State></Page> }
function SessionDetails() { const { id } = useParams(); const [session, setSession] = useState(null); const [error, setError] = useState(''); const [message, setMessage] = useState(''); useEffect(() => { api.session(id).then(setSession).catch(e => setError(e.message)) }, [id]); const book = () => api.createBooking(id).then(() => setMessage('Your spot is reserved.')).catch(e => setError(e.message)); const s = session || fallbackSessions.find(x => String(x.id) === id); return <Page title={s?.title || 'Session details'} subtitle="Everything you need before you step onto the pitch."><State error={error} empty={!s}><div className="grid gap-6 lg:grid-cols-[1fr_360px]"><div className="rounded-2xl border border-slate-200 bg-white p-7"><Link to="/sessions" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ChevronLeft className="h-4 w-4" /> Back to sessions</Link><h2 className="mt-8 text-2xl font-black">{s.title}</h2><div className="mt-6 grid gap-4 text-sm"><DetailRow icon={<MapPin />}>{`${s.venue || s.venue_name || 'Venue'}${s.location ? ` · ${s.location}` : ''}`}</DetailRow><DetailRow icon={<CalendarDays />}>{s.date || s.start_date || 'Date to be announced'}</DetailRow><DetailRow icon={<Clock3 />}>{s.time || s.start_time || 'Time to be announced'}</DetailRow></div><p className="mt-8 leading-7 text-slate-500">{s.description || 'Join fellow cricket players for a friendly, organised game. Bring your gear and arrive 15 minutes early.'}</p></div><div className="h-fit rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="text-sm text-slate-500">Entry fee</div><div className="mt-1 text-3xl font-black">{s.price || s.fee || 'Free'}</div><button onClick={book} className="mt-6 w-full rounded-xl bg-emerald-600 py-3.5 font-extrabold text-white hover:bg-emerald-700">Reserve my spot</button>{message && <p className="mt-4 text-sm font-semibold text-emerald-700">{message}</p>}</div></div></State></Page> }
function Auth({ register = false }) { const navigate = useNavigate(); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const submit = async e => { e.preventDefault(); setLoading(true); setError(''); const form = new FormData(e.currentTarget); const credentials = { email: form.get('email'), password: form.get('password') }; try { if (register) { await authApi.register({ full_name: form.get('full_name'), ...credentials }); const loginResult = await authApi.login(credentials); setStoredAuth(loginResult) } else { const result = await authApi.login(credentials); setStoredAuth(result) } navigate('/dashboard') } catch (err) { setError(err instanceof Error ? err.message : String(err)) } finally { setLoading(false) } }; return <main className="grid min-h-[calc(100vh-64px)] place-items-center bg-slate-50 px-5 py-12"><div className="w-full max-w-md"><div className="mb-7 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-emerald-600 text-2xl">🏏</div><h1 className="mt-5 text-3xl font-black">{register ? 'Create your account' : 'Welcome back'}</h1><p className="mt-2 text-sm text-slate-500">{register ? 'Join local players and start playing.' : 'Sign in to manage your cricket activity.'}</p></div><form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8">{register && <Field name="full_name" label="Full name" placeholder="Shubham Kumar" />}<Field name="email" label="Email" type="email" placeholder="you@example.com" /><Field name="password" label="Password" type="password" placeholder="Your password" />{error && <p className="mb-4 whitespace-pre-line rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="mt-2 w-full rounded-xl bg-slate-950 py-3.5 text-sm font-extrabold text-white hover:bg-slate-800 disabled:opacity-60">{loading ? 'Please wait…' : register ? 'Create account' : 'Log in'}</button><div className="mt-5 flex items-center justify-center gap-1 text-sm text-slate-500">{register ? 'Already have an account?' : "Don't have an account?"}<Link to={register ? '/login' : '/register'} className="font-bold text-emerald-600">{register ? 'Log in' : 'Create one'}</Link></div></form><div className="mt-5 flex justify-center gap-5 text-xs text-slate-400"><span className="flex items-center gap-1"><ShieldCheck className="h-3.5 w-3.5" /> Secure account</span><span className="flex items-center gap-1"><Trophy className="h-3.5 w-3.5" /> Cricket community</span></div></div></main> }
function Field({ name, label, type = 'text', placeholder }) { return <label className="mb-4 block text-sm font-bold text-slate-700">{label}<input name={name} type={type} placeholder={placeholder} required className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none placeholder:text-slate-300 focus:border-emerald-500 focus:ring-4 focus:ring-emerald-500/10" /></label> }
function Protected({ children }) { return getStoredUser() ? children : <Navigate to="/login" replace /> }
function Dashboard() { const user = getStoredUser(); return <Page title={`Welcome back${user?.name ? `, ${user.name.split(' ')[0]}` : ''}`} subtitle="Your cricket activity at a glance."><div className="grid gap-5 sm:grid-cols-3"><Stat icon={<CalendarDays />} label="Upcoming bookings" value="—" /><Stat icon={<MapPin />} label="Saved venues" value="—" /><Stat icon={<Users />} label="Players connected" value="—" /></div><div className="mt-7 grid gap-5 lg:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-white p-7"><h2 className="text-xl font-black">Ready to play?</h2><p className="mt-2 text-sm leading-6 text-slate-500">Find a session that matches your schedule and skill level.</p><Link to="/sessions" className="mt-6 inline-flex items-center gap-2 rounded-xl bg-emerald-600 px-5 py-3 text-sm font-extrabold text-white">Find sessions <ArrowRight className="h-4 w-4" /></Link></div><div className="rounded-2xl border border-slate-200 bg-white p-7"><h2 className="text-xl font-black">Your profile</h2><p className="mt-2 text-sm leading-6 text-slate-500">Keep your player details current so teams can find you.</p><Link to="/profile" className="mt-6 inline-flex items-center gap-2 rounded-xl border border-slate-200 px-5 py-3 text-sm font-extrabold">View profile</Link></div></div></Page> }
function Provider() { return <Page title="Provider dashboard" subtitle="Manage venues, sessions and bookings."><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4"><Stat icon={<CalendarDays />} label="Sessions" value="—" /><Stat icon={<MapPin />} label="Venues" value="—" /><Stat icon={<Users />} label="Bookings" value="—" /><Stat icon={<CircleUserRound />} label="Profile" value="—" /></div><div className="mt-7 rounded-2xl border border-slate-200 bg-white p-7"><h2 className="text-xl font-black">Grow your cricket community</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Use the backend provider tools to publish venues and sessions for local players.</p><Link to="/profile" className="mt-6 inline-flex rounded-xl bg-slate-950 px-5 py-3 text-sm font-bold text-white">Manage profile</Link></div></Page> }
function Profile() { const user = getStoredUser(); return <Page title="Profile" subtitle="Your player identity on CricketConnect."><div className="max-w-2xl rounded-2xl border border-slate-200 bg-white p-7"><div className="flex items-center gap-4"><div className="grid h-14 w-14 place-items-center rounded-full bg-emerald-100 text-emerald-700"><UserRound /></div><div><h2 className="text-xl font-black">{user?.name || user?.email || 'Cricket player'}</h2><p className="text-sm text-slate-500">{user?.email || 'Add your email in the backend profile.'}</p></div></div></div></Page> }
function Stat({ icon, label, value }) { return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-50 text-emerald-600 [&>svg]:h-5 [&>svg]:w-5">{icon}</div><span className="text-2xl font-black">{value}</span></div><div className="mt-4 text-sm font-bold text-slate-500">{label}</div></div> }

function App() { return <Layout><Routes><Route path="/" element={<Home />} /><Route path="/sessions" element={<Sessions />} /><Route path="/sessions/:id" element={<SessionDetails />} /><Route path="/venues" element={<Venues />} /><Route path="/bookings" element={<Protected><Bookings /></Protected>} /><Route path="/login" element={<Auth />} /><Route path="/register" element={<Auth register />} /><Route path="/dashboard" element={<Protected><Dashboard /></Protected>} /><Route path="/provider" element={<Protected><Provider /></Protected>} /><Route path="/profile" element={<Protected><Profile /></Protected>} /></Routes></Layout> }
export default App
